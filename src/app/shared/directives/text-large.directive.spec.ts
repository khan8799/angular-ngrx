import { TextLargeDirective } from './text-large.directive';

describe('TextLargeDirective', () => {
  it('should create an instance', () => {
    const directive = new TextLargeDirective();
    expect(directive).toBeTruthy();
  });
});
